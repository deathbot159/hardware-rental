enum APIResponseStatus {
    ERROR = -1,
    SUCCESS,
    INVALID_CREDENTIALS,
    INVALID_TOKEN,
    DATA_NOT_FOUND,
    INVALID_PERMISSIONS,
    INVALID_REQUEST_BODY
}

export default APIResponseStatus;